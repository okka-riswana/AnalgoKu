% % % % % % % % % %
%
% Style file for assignments
% Author: Okka Riswana
%
% % % % % % % % % %

\ProvidesPackage{assignments}


% % % % % % % % % %
%
% 3rd party packages
%
% % % % % % % % % %

\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{amsmath}
\usepackage{amstext}
\usepackage{fancyvrb}
\usepackage{hyperref}
\usepackage[main=indonesian,english]{babel}
\usepackage[backend=bibtex,style=authoryear]{biblatex}
\usepackage{tocloft}
\usepackage{titlesec}
\usepackage{minted}
\usepackage{tikz}
\usepackage{array}
%\usepackage[a4paper, margin=1in]{geometry}


% % % % % % % % % %
%
% General styling and configurations
%
% % % % % % % % % %

\pagenumbering{arabic}
\linespread{1.3}
\parindent 0in
\parskip 1em
\hypersetup{
    colorlinks,
    citecolor=black,
    filecolor=black,
    linkcolor=black,
    urlcolor=blue
}
\usetikzlibrary{automata, positioning, arrows}
\tikzset{
    ->, % makes the edges directed
    >=stealth, % makes the arrow heads bold
    node distance=3cm, % specifies the minimum distance between two nodes. Change if necessary.
    every state/.style={thick, fill=gray!10}, % sets the properties for each ’state’ node
    initial text=$ $, % sets the text that appears on the start arrow
}


% % % % % % % % % %
%
% ToC, LoF, LoT
%
% % % % % % % % % %

% ToC
\addto\captionsindonesian{\renewcommand\tableofcontents{Daftar Isi}}
\renewcommand{\cfttoctitlefont}{\hfill\large\bfseries}
\renewcommand\cftaftertoctitleskip{1\baselineskip}
\renewcommand{\cftaftertoctitle}{%
  \hfill\null\\[2\baselineskip]\bfseries No.\hfill\phantom{Hal}Judul\phantom{No.}\hfill Hal}

% LoF
\addto\captionsindonesian{\renewcommand\listfigurename{Daftar Gambar}}
\renewcommand{\cftloftitlefont}{\hfill\large\bfseries}
\renewcommand\cftafterloftitleskip{1\baselineskip}
\renewcommand{\cftafterloftitle}{%
  \hfill\null\\[2\baselineskip]\bfseries No.\hfill\phantom{Hal}Judul\phantom{No.}\hfill Hal}


% LoT
\addto\captionsindonesian{\renewcommand\listoftables{Daftar Tabel}}
\renewcommand{\cftlottitlefont}{\hfill\large\bfseries}
\renewcommand\cftafterlottitleskip{1\baselineskip}
\renewcommand{\cftafterlottitle}{%
  \hfill\null\\[2\baselineskip]\bfseries No.\hfill\phantom{Hal}Judul\phantom{No.}\hfill Hal}

\renewcommand\cftfigindent{0pt}

% Article TOC Fonts
\renewcommand\cftsecfont{\normalsize}
\renewcommand\cftsecpagefont{\normalsize}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}

% Report TOC Fonts
% \renewcommand\cftchappagefont{\normalsize}
% \renewcommand\cftchapfont{\normalsize}
% \renewcommand{\cftchapleader}{\cftdotfill{\cftdotsep}}


% % % % % % % % % %
%
% Sections and Chapters
%
% % % % % % % % % %

% Section font size
\titleformat*{\section}{\Large\bfseries}
\titleformat*{\subsection}{\large\bfseries}
\titleformat*{\subsubsection}{\normalsize\bfseries}
\titleformat*{\paragraph}{\normalsize\bfseries}
\titleformat*{\subparagraph}{\normalsize\bfseries}


% % % % % % % % % %
%
% Bibliography
%
% % % % % % % % % %

\addbibresource{references.bib}


% % % % % % % % % %
%
% Command and environments
%
% % % % % % % % % %

% Source code
\newenvironment{code}[2][text]
 {\VerbatimEnvironment
  \begin{listing}[htp]
  \centering
  \begin{minted}[frame=lines,framesep=2mm,linenos,label=#2]{#1}}
 {\end{minted}\end{listing}}
 
 % Problem and solutions
 
\newcounter{problem}
\newcounter{solution}

\newcommand\Problem{%
  \stepcounter{problem}%
  \textbf{\theproblem.}~%
  \setcounter{solution}{0}%
}

\newcommand\TheSolution{%
  \textbf{Jawaban:}\\%
}

\newcommand\ASolution{%
  \stepcounter{solution}%
  \textbf{Jawaban \thesolution:}\\%
}

% Math mode table. L, C and R are the equivalent of l, c, and r

\newcolumntype{L}{>{$}l<{$}}
\newcolumntype{C}{>{$}c<{$}}
\newcolumntype{R}{>{$}r<{$}}